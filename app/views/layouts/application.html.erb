<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render :partial => "shared/head" %>
  </head>
  <body>
    <nav class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <ul class="nav navbar-nav">
          <li class="<%= navbar_link_class(:teams) %>"><%= link_to "Teams", teams_path %></li>
          <li class="<%= navbar_link_class(:buckets) %>"><%= link_to "Buckets", buckets_path %></li>
          <% if current_user.buckets.any? %>
            <li>
              <%= form_tag prioritized_issues_path, :class => "issue-import-form" do %>
                <%= text_field_tag :url, "https://github.com/hoytus/acme/issues/1", :class => "issue-import-url js-issue-import-url" %>
                <%= submit_tag "Import Issue" %>
              <% end %>
            </li>
          <% end %>
          <li>
            <a title="+Issue" href="javascript:void((function(){var%20e=document.createElement('script');e.setAttribute('type','text/javascript');e.setAttribute('src','<%= asset_url "bookmarklet.js" %>');document.body.appendChild(e)})())">+Issue</a>
          </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Logout", logout_path %></li>
        </ul>
      </div>
    </nav>


    <div class="container-fluid">
      <%= yield %>

      <footer>
      </footer>
    </div>
  </body>
</html>
